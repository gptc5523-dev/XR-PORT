# 프로젝트 코드 분석 리포트

> 작성일: 2026-06-09
> 분석 대상: 팀 자체 코드 **46개 파일** (`Assets/Crane/Sts`, `Assets/Container`, `Assets/Scripts`, `Assets/Shared`)
> 제외: `Assets/Samples`·`Assets/TutorialInfo` (Unity 패키지 샘플 = 벤더 코드)
> 검증: 모든 항목을 `Assets/` 트리 전체(`.cs`/`.unity`/`.prefab`/`.asset`)에 대해 grep 교차 검증

---

## 🔴 1. 데드코드 — 통째로 죽은 코드 (삭제 후보)

### 컨테이너 서브시스템 고아 상태 (최우선)

| 파일 | 상태 | 근거 |
|---|---|---|
| `Assets/Scripts/Editor/SpawnContainers.cs` (347줄) | **전체 데드** | 유일한 호출자 `ContainerFbxExporter.cs`가 삭제됨(git `D`). 현재 호출 0건. `BuildContainer`/`MakeSpec`/`BuildReeferUnit`/`AddBox`/`MakeChild`/`ApplyMaterial`/`MulColor` 전부 도달 불가 |
| `Assets/Container/Scripts/ContainerSpawner.cs` | **고아** | 어느 씬/프리팹에도 부착 안 됨. `SpawnAll()` 호출자 없음. `StsCraneCreator.cs:2977`이 리플렉션으로 `transform.position`만 읽음 |
| `Assets/Container/Scripts/ContainerInstance.cs` | **런타임 데드** | `Container_20ft.prefab`만 참조하나 그 프리팹은 어느 씬에도 배치 안 됨. 실제 경로는 `VRTestMenu`→`ProceduralContainerMesh` |
| `Assets/Crane/Scripts/Sts/Editor/CranePlayerStartPointMenu.cs` | **전체 데드** | `[MenuItem]` 주석 처리됨(보류 2026-06-04), 호출자 0건 |

> ⚠️ `ContainerSpawner`/`ContainerInstance`/`ColorPalette`/`IdGenerator` + `Container_20ft.prefab`은 실사용 경로(`VRTestMenu`→`ProceduralContainerMesh`→`MeshBuilder`)에서 완전히 빠져 있음. 삭제 전 보류 항목인지 확인 필요.

### 개별 데드 심볼

- `StsCrane.cs:28` — `public Transform Boom` 프로퍼티 + 백킹 필드 `boom`: 읽는 곳 없음 (씬의 `Boom_*`는 동명 GameObject)
- `SpreaderGrabber.cs:156` — `ToggleGrab()`: 호출자 없음. 클래스 주석(line 10)이 "ToggleGrab 호출"이라 적혀 있으나 실제론 Grab/Release 직접 호출 → 주석이 거짓
- `ContainerInstance.cs:54` — `Apply(string, Color, string)`: 호출자 0건
- `ContainerIdGenerator.cs:72` — `Validate(string)`: 호출자 0건
- `ContainerColorPalette.cs:37` — `Get(int)`: 호출자 0건
- `CubeReset.cs:34` — `SetPlacementMode(PlacementMode)`: 호출자 0건
- `ProceduralContainerMesh.cs:87` — `const float RoofCamber` (주석에 "미사용"), `:28` `HeightHC`: 사용 안 함
- `ContainerIdGenerator.cs:3` — `using System.Text;`: `StringBuilder` 미사용, 제거 가능
- `CranePartLabels.cs` — `Kind.Static` 라벨 호출 주석 처리(`:102-106`)로 `role` 필드, `staticLookAngle`(`:39`), `:190` if-분기, `:257` switch-case가 연쇄 도달 불가

---

## 🟠 2. 중복코드

### 프로젝트 공용화 후보 (여러 파일 교차)

- **`MulColor` 3중 정의** — `VRTestMenu.cs:266`, `SpawnContainers.cs:346` 등 `new Color(c.r*k, c.g*k, c.b*k, c.a)` 동일
- **`Has`/`NameHas` 부분문자열 헬퍼 3중** — `CraneControllerArrowHUD.cs:159`, `CraneModeSelectorHUD.cs:121`, `CraneHud.cs:276`
- **`Hash(int,int)` 정수해시 바이트단위 동일** — `StsCraneCreator.cs:2959`, `StsQuayGroundCreator.cs:267`
- **AABB 바운드 헬퍼 3중** — `SpreaderGrabber.cs:256` `TryBounds`, `CranePlayerStartPlacer.cs:97` `TryWorldBounds`, +`StsQuayGroundCreator`(주석이 "동일 방식"이라 자인)
- **URP 머티리얼 셋업 보일러플레이트 5곳** — `StsCraneCreator.GetMaterial`, `StsQuayGroundCreator.MakeMat`, `VRTestMenu.MakeMat`, `SpawnContainers.ApplyMaterial`, `NetLanSetup.MakeVisual` (`_BaseColor/_Color/_Metallic/_Smoothness` 탐색 동일)
- **콜라이더 제거 프리미티브 생성 4곳** — `NewPrimitive`/`PbBox`/`QuayBox`/`MakeVisual` 동일 시퀀스
- **삼각형 와인딩 보정 idiom 3중** — `StsCraneCreator.cs:2722`(`AddBevQuad`), `:2733`(`AddBevTri`), `StsQuayGroundCreator.cs:199`(`AddQuad`)
- **`AddFace`/`AddFlatQuad`** — `MeshBuilder.cs:67` private vs `ProceduralContainerMesh.cs:296` static 재노출, 동일 패턴

### 영역 내 중복

- **3개 무버 축 쓰기** — `GantryMover.cs:22`(z), `TrolleyMover.cs:24`(x), `SpreaderHoist.cs:33`(y): 축만 다른 동일 5줄 → `AxisMoverBase`로 통합 가능
- **로프 실린더 배치 수학** — `HoistRopeRig.cs:60`, `BoomRopeRig.cs:98` 동일
- **속도 평활화 로직** — `CraneStatusHUD.cs:126`, `CranePartLabels.cs:219`: `0.15f` 시정수·`*60f` 복붙
- **헤드락 부착/재부착** — `CraneStatusHUD.TryAttachToCamera`(`:138`), `CraneControlHintsHUD`(`:60`) 동일
- **진단 스캐너** — `CraneControllerArrowHUD.Candidates`(`:162`), `CraneModeSelectorHUD.RightSideCandidates`(`:124`) 거의 동일
- **퍼센트/바 위치 수학** — `CraneStatusHUD.cs:222`, `CranePartLabels.cs:271` 동일
- **VRTestMenu 삭제→스폰→FrameSelected 보일러플레이트** — `SpawnBothSizes`/`SpawnSingle`/`SpawnProcedural2x2`/`PlaceContainersOnQuay` 3~4회 반복
- **스프레더 시브 지오메트리** — `StsCraneCreator.cs:974`, `:1519` 인라인 vs `SheaveNest`(`:1927`) 중복

---

## 🟡 3. 기타 이슈

### 항상 켜진 디버그 로그 (빌드 스팸)
`debugLog = true` / `const bool DebugLog = true` 기본값으로 빌드에서도 로그 발생:
- `SpreaderGrabber.cs:26` (로그 5곳), `StsCraneVRController.cs:60` (로그 9곳), `CranePlayerRigScale.cs:30`, `CranePlayerStartPlacer.cs:24`
- `XRTeleportActionDisabler.cs:17`, `XRTurnAroundDisabler.cs:18` (const라 `if(DebugLog)` 가드가 항상-참 죽은 분기)
- `ContainerPhysics.cs:69` (AutoSpawn 컴포넌트라 인스펙터로 끌 방법 없음 → 항상 로그)

### 보안
- `AndroidKeystoreAutoFill.cs:19-21` — 키스토어 평문 비밀번호(`"container123"`) 하드코딩. 저장소 공유 시 위험 (파일 주석도 자인)

### 인스펙터 값 무력화하는 매직넘버
- `SpreaderGrabber.cs:202` — `Mathf.Min(passXZmargin, 0.03f)`로 직렬화 필드(0.1) 무력화
- `SpreaderGrabber.cs:227` — `Mathf.Min(grabRange, maxGrabRange)`로 `grabRange`(0.35) 항상 덮어씀
- `SpreaderGrabber.cs:49` — `const MinRescanInterval = 3f`가 `refreshInterval`(0.5) 무력화

### 기타
- 포트 기본값 드리프트 — `NetLanUI.cs:20` `port=7777` vs `LanDiscovery.cs:118` 폴백 `7777` 별도 리터럴
- `PbBox`의 `bevel` 인자 무시 — `StsCraneCreator.cs:2780` `_ = bevel`, 모든 호출부가 의미 없는 값 전달
- 정적 캐시 비대칭 — `StsCraneCreator.cs`: init에서 `_ropeTex` 리셋하나 cleanup 미해제(누수), `_beveledCube`/`_unitQuad` 무효화 안 함
- 대량 주석처리 지오메트리 블록 — `StsCraneCreator.cs` 9개 구간(`452`, `636`, `913`, `1790-1819` 등). "주석 해제로 복구" 워크플로우상 의도된 토글
- `ProceduralContainerMesh.cs:20-22, 127` — 가변 static `Length/Width/Height` + `BuildSized` save/restore, 단일스레드 가정 의존 (재진입 시 손상 위험)
- `CranePartLabels.cs:226`, `CraneStatusHUD.cs:133` — 평활화 시정수 `0.15f` 네임드 const화 권장

---

## ✅ 권장 정리 순서

1. **즉시 안전 삭제**: `SpawnContainers.cs`, `CranePlayerStartPointMenu.cs`, `using System.Text`(IdGenerator), `RoofCamber`/`HeightHC`, `StsCrane.Boom`, `ToggleGrab()`
2. **확인 후 삭제**: 컨테이너 고아 서브시스템(`ContainerSpawner`/`ContainerInstance`/`ColorPalette`/`IdGenerator`) — 보류 항목 여부 확인
3. **공용화**: `MulColor`/`Has`/`Hash`/AABB/머티리얼셋업 → 공통 유틸, 3개 무버 축쓰기 → `AxisMoverBase`
4. **빌드 위생**: `debugLog` 기본값 `false`, 키스토어 비밀번호 외부화

> 데드코드 삭제는 회사 방침상 git 대신 폴더 백업(`Container_v*`) 후 진행.
