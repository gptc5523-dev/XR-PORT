# 보류 항목 (Deferred / TODO)

사용자가 **일단 보류**한 기능·결정 항목 모음. 각 항목은 다시 요청하기 전까지 건드리지 않는다.
재개할 때 바로 이어갈 수 있도록 **무엇 / 왜 보류 / 재개 방향 / 관련 파일**을 정리한다.

> 참고: 크레인 *형상(메시) 코드* 보류 블록은 별도 문서 `Assets/Crane/Scripts/Sts/DEFERRED_DETAILS.md`에 있다. 이 문서는 *기능·결정* 보류용.

최종 갱신: 2026-06-04

---

## 1. 플레이어 리그 스케일 (시작 위치 기능 포함)  — 보류 2026-06-04

**무엇:** VR 시작 시 플레이어가 부두 바닥이 아니라 모델 위 공중에 떠 보임.

**원인:** 씬(크레인·부두)은 전부 **1/24 축소 모델**(`StsCrane.ModelScale = 1/24`)인데, **XR 리그(플레이어)는 스케일 1(실척)** 이라 거인이 미니어처를 내려다보는 상태. 발을 바닥(y≈0)에 붙여도 눈높이가 24배라 솟음. 씬엔 `ARPlaneManager`/`TablePlacementManager` 등 MR 탁상 셋업도 있음.

**재개 방향 (결정 필요):**
- (A) XR 리그를 1/24로 축소 → 사람 키로 부두에 서기 (사람 시점)
- (B) 현재 스케일 유지 → 탁상 미니어처(거인 시점)

**연결된 작업(같이 일시정지):** 시작 위치 지정 기능.
- 구현됨: `CranePlayerStartPoint`(마커), `CranePlayerStartPlacer`(마커 위치·방향에서 시작, 바닥 자동 보정)
- 에디터 메뉴 `플레이어 시작 지점 생성`은 **주석 처리(숨김)** — `CranePlayerStartPointMenu.cs`의 `[MenuItem]` 한 줄만 풀면 복구

**관련 파일:** `Assets/Crane/Scripts/Sts/Runtime/CranePlayerStartPlacer.cs`, `CranePlayerStartPoint.cs`, `Assets/Crane/Scripts/Sts/Editor/CranePlayerStartPointMenu.cs`

---

## 2. 스프레더 통과 방지 / 수평 충돌  — 보류 2026-06-04

**무엇:** 스프레더를 컨테이너 높이에서 옆으로 이동시키면, 물리 충돌(부딪혀 멈춤) 대신 **자동으로 컨테이너 윗면으로 솟아오름.** 사용자는 "물리 충돌이 있어야 한다"고 지적.

**원인:** `SpreaderGrabber.cs`의 `LateUpdate` `blockPassThrough`(약 156~213행). 빈 스프레더 부착점 XZ 중심이 컨테이너 footprint 안 + 윗면보다 낮으면 호이스트를 윗면까지 자동 상승(212행 `hoist.MoveTo`). 스프레더 시각 파츠는 콜라이더가 없어서(`NewPrimitive`가 제거), 위에서 뚫고 들어가는 걸 막으려고 코드로 대체한 것. **수직만** 막아서 옆에서 들어가면 타넘는 부작용.

**재개 방향 (결정 필요):**
- (A) 수평 충돌 시 트롤리/갠트리 정지 추가 — 가장 현실적, **추천**
- (B) `blockPassThrough` 끄기 — 자동 상승은 사라지나 콜라이더가 없어 그냥 통과(비추천)
- (C) 스프레더에 실제 콜라이더 부여 — 진짜 충돌이지만 kinematic이라 컨테이너를 밀어내는 부작용

**관련 파일:** `Assets/Crane/Scripts/Sts/Runtime/SpreaderGrabber.cs`

---

## 3. 자동 설치 감시 스크립트  — 보류 2026-06-01

**무엇:** Quest 연결 감지 시 자동으로 APK install 하는 감시 스크립트.

**왜 보류:** 사용자가 보류. 요청 전 꺼내지 말 것.

**재개 방향:** 사용자가 다시 요청하면 구현.

---

## 4. 크레인 운전실 진입 존 (VR 모드전환 2번)  — 예정

**무엇:** VR 조종 모드전환 방식 2번 — 운전실(cab) 트리거 존에 들어가면 조종 모드로 전환.

**왜 보류:** 현재 1번(버튼 토글) 방식 테스트 후 구현 예정.

**재개 방향:** 1번 버튼 토글이 안정적으로 검증되면 운전실 트리거 존 추가.

---

## 5. ML 모델 객체 인식  — 향후(대규모)

**무엇:** Quest 3 Camera Raw + Unity Sentis로 임의 객체 인식.

**왜 보류:** 작업 규모가 큼. 향후 도입.

**재개 방향:** 별도 큰 작업으로 착수.

---

## 참고: 숨겨둔(주석 처리한) 에디터 메뉴

`Container/` 메뉴에서 미사용이라 `[MenuItem]`만 주석 처리한 항목들 (복구하려면 주석 해제):
- `플레이어 시작 지점 생성` — 위 #1과 연동, 보류
- `임포트 항만 크레인 배치` — 사용자 요청으로 숨김 (`HarborCraneSpawner.cs`)
- `컨테이너 프리팹 생성`, `절차 메시 재생성` — prefab 파이프라인 휴면(미사용), 실제 컨테이너는 절차 메시로 즉석 생성 (`ContainerEditorBuilder.cs`)
