# STS 크레인 속도 정리 (2026-06-09)

값은 **실척 체감 m/s**이며, 런타임에 `crane.ModelScale(1/24)`을 곱해 모델 단위로 환산됨
(`StsCraneVRController`: `MoveTo(Current + stick * speedMps * ModelScale * Time.deltaTime)`).
모델은 작아도 "실제 크레인이 그 거리를 지나는 데 걸리는 시간(초)"은 현실과 동일.

정의 위치: `Assets/Crane/Scripts/Sts/Runtime/StsCraneVRController.cs` (라인 40~50) + 씬 `SampleScene.unity` 직렬화 값.

## 속도 평가표

| 동작 | 현재값(m/s) | = m/min | 현실 STS 일반치 | 평가 |
|---|---|---|---|---|
| **갠트리**(크레인 주행) | 0.75 | 45 | 40~45 m/min | ✅ 정확(교과서적). 갠트리는 원래 가장 느림 |
| **호이스트 적재** | 1.3 | 78 | 50~90 m/min | ✅ 현실적 |
| **호이스트 공차** | 2.7 | 162 | 120~180 m/min | ✅ 현실적 |
| **트롤리** | **3.3** | **198** | 150~240 m/min | ✅ 현실적(중상). 240(=4.0)에서 묵직하게 하향 |
| 걷기(플레이어) | 8.0 | — | (크레인 아님) | ⚠ 전력질주급(≈29km/h). 넓은 부두용, 취향 |

## 종합
- 크레인 작동 속도(갠트리/호이스트/트롤리) 모두 현실적 범위.
- **호이스트 공차:적재 ≈ 2.7:1.3 ≈ 2:1** — 실제 크레인(빈 스프레더가 적재보다 ~2배 빠름)과 정확히 일치.
- **갠트리 45 m/min** — 실제 STS 표준치와 거의 동일.
- 트롤리는 빠른 편 상한(240)에서 198 m/min으로 낮춰 체감 안정화.
- 걷기 8 m/s는 플레이어 로코모션(크레인 무관). VR에서 속도 위화감 시 값보다 **리그 스케일 ★미해결**(플레이어 거대 → 세상 작고 빠르게 보임)을 의심.

## 변경 이력
- 2026-06-09: 트롤리 `4.0 → 3.3` m/s (240 → 198 m/min). 코드 기본값 + 씬 직렬화 값 동시 변경.
